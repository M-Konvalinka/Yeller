import React from 'react';

const notifications = () => (
    <div>
        <h1> Here is test info for the routes, this would render the notifications page </h1>
    </div>
)

export default notifications;